import 'package:flutter/material.dart';

class MediaCategoryCard extends StatelessWidget {
  const MediaCategoryCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}